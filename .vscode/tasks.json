{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tests",
            "type": "shell",
            "command": "poetry run pytest --cov --cov-report html ",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}
